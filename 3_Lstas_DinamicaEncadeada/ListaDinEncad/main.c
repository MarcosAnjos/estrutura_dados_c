#include <stdio.h>
#include <stdlib.h>

int main()
{

        Lista *li; //ponteiro para ponteiro
        li = cria_lista();
        liberaLista(li);

        int x = tamanhoLista(li);

        int x = listaCheia(li);
        if(listaCheia(li));

        int x = listaVazia(Lista* li);
        if(listaVazia(li));

        int x = insereListaInicio(li, dados_aluno);

        int x = inreseListaFinal(li, dados_aluno);

        int x = removeListaInicio(li);

        int x = removeListaFinal(li);

        int x = removeLista(li, matricula_aluno);

        int x = consultaListaPosicao(li, posicao, &dados_aluno);

        int x = consultaMatricula(li, matricula,&dados.matricula );


}

