#include <stdio.h>
#include <stdlib.h>
#include "ListaSequencial.h"



int main()
{


    Lista *li;

    li = cria_lista();
    liberaLista(li);
    int x = listaCheia(li);
        if(listaCheia(li));

    int x = listaVazia(li);
        if(listaVazia(li));

    int x = insereListaFinal(li, dados alunos);
    int x = insereListaInicio(li, dados alunos);
    int x = insereListaOrdenada(li, dados alunos);

    int x = removeListaFinal(li);
    int x = removeListaInicio(li);
    int x = removeLista(li, matricula aluno);



}
