#include <stdio.h>
#include <stdlib.h>

int main()
{

    Lista *li;
    li = criaLista();
    liberaLista(li);

    int x = tamanhoLista(li);

    int x = listaCheia(li);

    int x = ListaVazia(Lista* li);

    int x = insereListaInicio(li, dados_alunos);

    int x = insereListaFinal(li, dados_aluno);

    int x = insereListaOrdenada(li, dados_alunos);

    int x = removeInicio(li);

    int x = removeFinal(li);

    int x = removeLista(li, matricula_aluno);

    int x = consultaPosicao(li, posicao, &dado_aluno);

    int x = cons

}



